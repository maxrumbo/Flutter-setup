import 'package:flutter/material.dart';

class HandwritingPage extends StatelessWidget {
  const HandwritingPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Handwriting Recognition')),
      body: const Center(
        child: Text('Handwriting recognition feature coming soon!'),
      ),
    );
  }
}
